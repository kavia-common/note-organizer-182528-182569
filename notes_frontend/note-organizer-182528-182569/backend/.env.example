# Backend environment variables for Ocean Notes

# Port the Express server will listen on (default: 4000)
PORT=4000

# CORS origin allowed to access the API (default: http://localhost:3000)
CORS_ORIGIN=http://localhost:3000

# Path to the SQLite database file, relative to backend root or absolute.
# Default if unset: ./data/notes.db
DB_PATH=./data/notes.db
